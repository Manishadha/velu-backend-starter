# Minimal Dockerfile for the packaged Velu project
# This is a starting point – feel free to adjust as needed.

FROM python:3.12-slim

WORKDIR /app

# System deps (optional; keep small and add only what is needed)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt /app/
RUN pip install --no-cache-dir -r requirements.txt

COPY . /app

# Default: run the generated FastAPI API
EXPOSE 8000
CMD ["uvicorn", "generated.services.api.app:app", "--host", "0.0.0.0", "--port", "8000"]
