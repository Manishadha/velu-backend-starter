# Packaged project: product_catalog_web_2

This zip was generated by Velu's packager.

It contains:
- generated/  : generated FastAPI services and Next.js frontend
- web/        : top-level Next.js app (if present)
- react_spa/  : Vite + React SPA (if present)
- src/        : core Python modules and helpers
- tests/      : pytest tests used during generation
- pyproject.toml / requirements.txt : Python project metadata
- README.md   : original Velu README from the source repo

This directory can act as a self-contained starting point for running
or adjusting the generated app.

============================
1. Create and activate a virtualenv
============================

    python -m venv .venv
    source .venv/bin/activate
    pip install -r requirements.txt

============================
2. Run the generated FastAPI backend
============================

Main API entrypoint (generated):
    generated/services/api/app.py

Command:
    uvicorn generated.services.api.app:app --reload --port 8000

============================
3. Run the Next.js frontend
============================

    cd generated/web
    npm install
    npm run dev -- --port 3001

Then open: http://localhost:3001

============================
4. Run tests
============================

From the project root (this directory):
    pytest -q

============================
5. Docker (optional)
============================

This archive also includes Dockerfile_packaged and
docker-compose.packaged.yml as simple starting points.

============================
6. Customizing the project
============================

- Edit Python modules under src/ and generated/services/
- Update React/Next.js pages under generated/web/, web/, or react_spa/
- Modify or add tests under tests/
- Iterate with Velu to generate new versions or modules.
