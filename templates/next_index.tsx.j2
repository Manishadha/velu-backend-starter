import React from "react";

interface ProductCard {
  name: string;
  price?: string;
  badge?: string;
}

interface Props {
  /* these are filled in by Velu’s packager / scaffold step */
  appTitle?: string;
  moduleName?: string;
  productType?: string;
  goal?: string;
  designStyle?: string;
  locales?: string[];
}

const demoProducts: ProductCard[] = [
  { name: "Example product A", price: "$29.00", badge: "New" },
  { name: "Example product B", price: "$59.00", badge: "Best seller" },
  { name: "Example product C", price: "$99.00", badge: "Popular" },
  { name: "Example product D", price: "$149.00" },
];

const demoCategories = [
  "All",
  "Home & Living",
  "Electronics",
  "Fashion",
  "Books",
  "More…",
];

export default function Home(props: Props) {
  const title =
    props.appTitle ||
    props.moduleName ||
    "Your product catalog";
  const subtitle =
    props.goal ||
    "Catalog + cart + secure checkout, generated by Velu.";
  const productType = props.productType || "ecommerce";
  const locales = props.locales && props.locales.length
    ? props.locales.join(", ")
    : "en";

  return (
    <main className="min-h-screen bg-slate-50">
      <header className="border-b border-slate-200 bg-white">
        <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-3">
          <div className="flex items-center gap-2">
            <div className="flex h-8 w-8 items-center justify-center rounded-lg bg-sky-600 text-xs font-bold text-white">
              V
            </div>
            <div className="flex flex-col leading-tight">
              <span className="text-sm font-semibold">{title}</span>
              <span className="text-[0.65rem] uppercase tracking-[0.2em] text-slate-500">
                {productType} • multi-channel
              </span>
            </div>
          </div>

          <nav className="hidden items-center gap-6 text-sm text-slate-600 md:flex">
            <a href="#catalog" className="hover:text-slate-900">
              Catalog
            </a>
            <a href="#features" className="hover:text-slate-900">
              Features
            </a>
            <a href="#about" className="hover:text-slate-900">
              About
            </a>
            <a href="#contact" className="hover:text-slate-900">
              Contact
            </a>
          </nav>

          <div className="flex items-center gap-3 text-sm">
            <button className="hidden rounded-full border border-slate-200 px-3 py-1 text-xs font-medium text-slate-700 hover:bg-slate-50 md:inline-flex">
              Sign in
            </button>
            <button className="relative inline-flex items-center gap-1 rounded-full bg-slate-900 px-3 py-1 text-xs font-medium text-white hover:bg-slate-800">
              <span>Cart</span>
              <span className="flex h-4 w-4 items-center justify-center rounded-full bg-white text-[0.65rem] font-semibold text-slate-900">
                0
              </span>
            </button>
          </div>
        </div>
      </header>

      {/* Hero */}
      <section className="border-b border-slate-200 bg-gradient-to-br from-sky-50 via-white to-slate-50">
        <div className="mx-auto grid max-w-6xl gap-8 px-6 py-10 md:grid-cols-[1.4fr,1fr] md:py-12">
          <div>
            <p className="mb-2 text-xs font-semibold uppercase tracking-[0.2em] text-sky-700">
              Velu generated project
            </p>
            <h1 className="mb-3 text-2xl font-bold text-slate-900 md:text-3xl">
              {title}
            </h1>
            <p className="mb-4 text-sm text-slate-600">{subtitle}</p>

            <div className="mb-4 flex flex-wrap items-center gap-2 text-[0.7rem] text-slate-600">
              <span className="rounded-full bg-slate-900 px-2 py-1 text-[0.7rem] font-medium text-white">
                FastAPI + Next.js
              </span>
              <span className="rounded-full bg-slate-100 px-2 py-1">
                DB ready (Postgres / SQLite)
              </span>
              <span className="rounded-full bg-slate-100 px-2 py-1">
                Locales: {locales}
              </span>
            </div>

            <div className="flex flex-wrap items-center gap-3">
              <button className="inline-flex items-center justify-center rounded-xl bg-sky-600 px-4 py-2 text-sm font-medium text-white shadow-sm hover:bg-sky-700">
                Start shopping
              </button>
              <button className="inline-flex items-center justify-center rounded-xl border border-slate-200 px-4 py-2 text-sm font-medium text-slate  -700 hover:bg-slate-50">
                View admin dashboard
              </button>
            </div>

            <p className="mt-4 text-xs text-slate-500">
              This is a starter frontend. Connect it to real products, carts,
              checkout, and auth using the generated API and database models.
            </p>
          </div>

          {/* Right side: "mock" storefront */}
          <div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm">
            <div className="mb-3 flex items-center justify-between text-xs text-slate-500">
              <span>Demo catalog</span>
              <span className="rounded-full bg-slate-100 px-2 py-0.5 text-[0.65rem]">
                Secure checkout
              </span>
            </div>
            <div className="grid grid-cols-2 gap-3">
              {demoProducts.map((p) => (
                <div
                  key={p.name}
                  className="flex flex-col rounded-xl border border-slate-100 bg-slate-50 p-2"
                >
                  <div className="mb-2 h-20 rounded-lg bg-gradient-to-br from-sky-100 to-slate-100" />
                  <div className="flex-1">
                    <p className="text-xs font-medium text-slate-900">
                      {p.name}
                    </p>
                    {p.price && (
                      <p className="text-xs font-semibold text-sky-700">
                        {p.price}
                      </p>
                    )}
                  </div>
                  <div className="mt-1 flex items-center justify-between">
                    {p.badge && (
                      <span className="rounded-full bg-white px-2 py-0.5 text-[0.6rem] font-medium text-sky-700">
                        {p.badge}
                      </span>
                    )}
                    <button className="rounded-full bg-slate-900 px-2 py-0.5 text-[0.65rem] font-medium text-white">
                      Add to cart
                    </button>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* Catalog */}
      <section id="catalog" className="mx-auto max-w-6xl px-6 py-8">
        <div className="mb-4 flex flex-wrap items-center justify-between gap-4">
          <div>
            <h2 className="text-lg font-semibold text-slate-900">
              Catalog
            </h2>
            <p className="text-sm text-slate-500">
              Replace this demo data with real products from your database.
            </p>
          </div>
          <div className="flex flex-wrap items-center gap-2 text-xs">
            {demoCategories.map((cat) => (
              <button
                key={cat}
                className="rounded-full border border-slate-200 bg-white px-3 py-1 text-xs text-slate-700 hover:bg-slate-50"
              >
                {cat}
              </button>
            ))}
          </div>
        </div>

        <div className="grid gap-4 md:grid-cols-3 lg:grid-cols-4">
          {demoProducts.concat(demoProducts).map((p, idx) => (
            <article
              key={`${p.name}-${idx}`}
              className="flex flex-col rounded-2xl border border-slate-200 bg-white p-3 shadow-[0_1px_3px_rgba(15,23,42,0.06)]"
            >
              <div className="mb-2 h-28 rounded-xl bg-gradient-to-br from-sky-100 to-slate-100" />
              <h3 className="text-sm font-semibold text-slate-900">
                {p.name}
              </h3>
              <p className="mt-1 text-xs text-slate-500">
                Short description placeholder. Map this to your real product copy.
              </p>
              {p.price && (
                <p className="mt-2 text-sm font-semibold text-slate-900">
                  {p.price}
                </p>
              )}
              <button className="mt-3 w-full rounded-xl bg-slate-900 px-3 py-2 text-xs font-medium text-white hover:bg-slate-800">
                View details
              </button>
            </article>
          ))}
        </div>
      </section>

      {/* Features */}
      <section
        id="features"
        className="border-y border-slate-200 bg-slate-900/95 py-8 text-slate-50"
      >
        <div className="mx-auto grid max-w-6xl gap-6 px-6 md:grid-cols-3">
          <div>
            <h3 className="text-sm font-semibold">Secure by default</h3>
            <p className="mt-1 text-xs text-slate-300">
              Add auth, JWT, roles, rate limiting and security headers on the
              backend. Velu templates are ready for hardened setups.
            </p>
          </div>
          <div>
            <h3 className="text-sm font-semibold">Multi-device</h3>
            <p className="mt-1 text-xs text-slate-300">
              Web + mobile. Use the generated API for Next.js, React Native,
              Expo or Flutter frontends.
            </p>
          </div>
          <div>
            <h3 className="text-sm font-semibold">Your database</h3>
            <p className="mt-1 text-xs text-slate-300">
              Connect real Postgres / SQLite / other databases. Velu does not
              host your data – you stay in full control.
            </p>
          </div>
        </div>
      </section>

      {/* About / contact */}
      <section id="about" className="mx-auto max-w-6xl px-6 py-8">
        <div className="grid gap-8 md:grid-cols-2">
          <div>
            <h3 className="text-sm font-semibold text-slate-900">
              About this project
            </h3>
            <p className="mt-2 text-sm text-slate-600">
              This storefront was generated automatically by Velu based on
              an intake conversation (product type, channels, security, and
              tech stack). You can now connect real APIs, auth flows, and
              database models for products, carts, and orders.
            </p>
          </div>
          <div id="contact">
            <h3 className="text-sm font-semibold text-slate-900">
              Contact / support
            </h3>
            <p className="mt-2 text-sm text-slate-600">
              Wire this section to your own contact form, support email, or
              CRM integration.
            </p>
            <div className="mt-3 flex flex-col gap-2 text-sm text-slate-600">
              <span>Email: support@example.com</span>
              <span>Phone: +00 000 0000</span>
            </div>
          </div>
        </div>
      </section>

      <footer className="border-t border-slate-200 bg-white">
        <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-3 text-xs text-slate-500">
          <span>© {new Date().getFullYear()} Your shop. All rights reserved.</span>
          <div className="flex gap-3">
            <a href="#" className="hover:text-slate-800">
              Terms
            </a>
            <a href="#" className="hover:text-slate-800">
              Privacy
            </a>
            <a href="#" className="hover:text-slate-800">
              Cookies
            </a>
          </div>
        </div>
      </footer>
    </main>
  );
}
